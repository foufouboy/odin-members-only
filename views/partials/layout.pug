doctype html
html
    head
        title= title
        meta(charset="utf-8")
        meta(name="viewport", content="width=device-width, initial-scale=1")
        link(rel='stylesheet', href='/styles/style.css')
        script(src="/scripts/index.js" defer)
        block scripts
    body
        include header
        block content
            main
                p I'm a sample main, with some text, but not that much
        include footer


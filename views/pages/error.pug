extends ../partials/layout

block content
    main.error
        h2 Oopsie.
        h3= `${error.statusCode} : ${message} `

        if typeof error === "string"
            h3= error
        else 
            p If you're the dev, check out the server logs!
            - 